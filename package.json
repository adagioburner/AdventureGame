{
  "name": "adventuregame",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p apps/web/tsconfig.json && tsc --noEmit -p apps/server/tsconfig.json && tsc --noEmit -p apps/server/test/tsconfig.json && tsc --noEmit -p tools/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:update-golden": "vitest run -u",
    "map": "node tools/balance/src/cli.ts map",
    "map:batch": "node tools/balance/src/cli.ts batch",
    "game": "node tools/balance/src/cli.ts game",
    "selfplay": "node tools/balance/src/cli.ts selfplay",
    "dev": "pnpm --filter @adventure/web dev",
    "dev:site": "pnpm --filter @adventure/web dev:site",
    "build:web": "pnpm --filter @adventure/web build",
    "build:site": "pnpm --filter @adventure/web build:site"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^6.0.0",
    "vitest": "^5.0.1"
  }
}
